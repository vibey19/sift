[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # numpy on macOS links against Apple Accelerate, whose randomized SVD path
    # raises spurious FP warnings on well-conditioned input. Does not occur on
    # the Linux build Vercel runs.
    ignore:.*encountered in matmul:RuntimeWarning
